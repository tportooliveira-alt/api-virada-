import { InstallGuide } from "@/components/InstallGuide";

// O título e o subtítulo desta tela vêm do AppShell (pageMeta), por isso aqui
// entra só o guia — que já se adapta ao aparelho de quem está lendo.
export default function InstalarPage() {
  return <InstallGuide />;
}
