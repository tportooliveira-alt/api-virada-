# ─── Google OAuth (Login + Sheets sync) ─────────────────────────────────────
# Crie em: console.cloud.google.com → APIs → Credenciais → OAuth 2.0
# Ative: Google Sheets API + Google Drive API
NEXT_PUBLIC_GOOGLE_CLIENT_ID=cole_aqui_o_client_id.apps.googleusercontent.com

# ─── Webhooks das plataformas de venda ───────────────────────────────────────
HOTMART_TOKEN=cole_token_aleatorio_aqui
# IDs/codigos/nomes (separados por virgula) que realmente liberam acesso ao app.
# Ex.: HOTMART_ALLOWED_PRODUCTS=H1234567A,app-codigo-da-virada,Codigo da Virada App
HOTMART_ALLOWED_PRODUCTS=
EDUZZ_TOKEN=cole_token_aleatorio_aqui
KIWIFY_TOKEN=cole_token_aleatorio_aqui
MONETIZZE_TOKEN=cole_token_aleatorio_aqui
CAKTO_TOKEN=cole_token_aleatorio_aqui
PERFECTPAY_TOKEN=cole_token_aleatorio_aqui

# ─── Painel admin (/admin/membros) ───────────────────────────────────────────
# Emails (csv) que podem acessar o painel admin. Validado pelo token Google no
# login; o servidor emite um cookie de sessão assinado só pra estes emails.
ADMIN_EMAILS=tportooliveira@gmail.com
# Segredo p/ assinar o cookie de sessão admin (HMAC). Use >= 32 chars aleatórios.
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SEM este segredo, o painel admin fica BLOQUEADO (fail-closed, por segurança).
ADMIN_SESSION_SECRET=

# ─── Pixels de marketing ─────────────────────────────────────────────────────
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_GTM_ID=
NEXT_PUBLIC_TIKTOK_PIXEL_ID=

# ─── IA (opcional, funcionalidades futuras) ──────────────────────────────────
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
